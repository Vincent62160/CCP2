{% extends 'baseA.html.twig' %}

{% block title %}Caracteristique{% endblock %}

{% block body %}
    <h1>Caracteristique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caracteristique.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ caracteristique.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('caracteristique_index') }}">back to list</a>

    <a href="{{ path('caracteristique_edit', {'id': caracteristique.id}) }}">edit</a>

    {{ include('caracteristique/_delete_form.html.twig') }}
{% endblock %}
