{% extends 'baseA.html.twig' %}

{% block title %}SectionEntretien{% endblock %}

{% block body %}
    <h1>SectionEntretien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section_entretien.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ section_entretien.titre }}</td>
            </tr>
            <tr>
                <th>Description1</th>
                <td>{{ section_entretien.description1 }}</td>
            </tr>
            <tr>
                <th>Description2</th>
                <td>{{ section_entretien.description2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('section_entretien_index') }}">back to list</a>

    <a href="{{ path('section_entretien_edit', {'id': section_entretien.id}) }}">edit</a>

    {{ include('section_entretien/_delete_form.html.twig') }}
{% endblock %}
