{% extends 'baseA.html.twig' %}

{% block title %}SectionDeux{% endblock %}

{% block body %}
    <h1>SectionDeux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ section_deux.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ section_deux.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('section_deux_index') }}">back to list</a>

    <a href="{{ path('section_deux_edit', {'id': section_deux.id}) }}">edit</a>

    {{ include('section_deux/_delete_form.html.twig') }}
{% endblock %}
