{% extends 'base.html.twig' %}

{% block title %}Message{% endblock %}

{% block body %}
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ message.Email }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ message.Sujet }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ message.Commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('message_index') }}">back to list</a>

    <a href="{{ path('message_edit', {'id': message.id}) }}">edit</a>

    {{ include('message/_delete_form.html.twig') }}
{% endblock %}
