{% extends 'baseA.html.twig' %}

{% block title %}New Section{% endblock %}

{% block body %}
   <div class="container mt-5">


    <h1>Créer section 1</h1>
    <a class="btn btn-primary" href="{{ path('Section_index') }}">Retour</a>
    <div class="row shadow p-3 mb-5 bg-white rounded">
    <div class=" col-12">

    {{ include('Section/_form.html.twig') }}

    <a href="{{ path('Section_index') }}">back to list</a>
{% endblock %}
